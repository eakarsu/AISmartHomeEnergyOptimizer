NODE_ENV=development
DATABASE_URL=
JWT_SECRET=
BACKEND_PORT=3001
FRONTEND_PORT=3000
CLIENT_URL=http://localhost:3000

# Optional local-only demo autofill. Keep disabled in production. Set a unique
# password in the ignored .env, then run: npm --prefix backend run provision:demo
ENABLE_DEMO_CREDENTIAL_AUTOFILL=false
SEED_ADMIN_EMAIL=admin@smarthome.invalid
SEED_ADMIN_PASSWORD=
